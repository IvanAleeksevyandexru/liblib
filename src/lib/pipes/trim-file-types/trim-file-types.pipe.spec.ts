import { TrimFileTypesPipe } from './trim-file-types.pipe';

describe('TrimFileTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimFileTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
