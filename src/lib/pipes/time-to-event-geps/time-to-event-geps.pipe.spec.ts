import { TimeToEventGepsPipe } from './time-to-event-geps.pipe';

describe('TimeToEventGepsPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToEventGepsPipe();
    expect(pipe).toBeTruthy();
  });
});
