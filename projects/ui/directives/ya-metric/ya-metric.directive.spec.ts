import { YaMetricDirective } from './ya-metric.directive';

describe('YaMetricDirective', () => {
  it('should create an instance', () => {
    const directive = new YaMetricDirective();
    expect(directive).toBeTruthy();
  });
});
