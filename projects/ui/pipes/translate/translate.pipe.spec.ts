import { LibTranslatePipe } from './translate.pipe';

describe('TranslatePipe', () => {
  /*it('create an instance', () => {
    const pipe = new LibTranslatePipe();
    expect(pipe).toBeTruthy();
  });*/
});
